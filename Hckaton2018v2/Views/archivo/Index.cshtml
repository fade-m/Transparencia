@model IEnumerable<Hckaton2018v2.archivo>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.ruta)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.ruta)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idArchivo }) |
                @Html.ActionLink("Details", "Details", new { id=item.idArchivo }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idArchivo })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>

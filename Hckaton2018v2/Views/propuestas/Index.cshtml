@model IEnumerable<Hckaton2018v2.propuestas>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.descripcion)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.archivo.ruta)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.candidato.idCandidato)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.tipoPropuesta.tipoPropuesta1)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.descripcion)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.archivo.ruta)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.candidato.idCandidato)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.tipoPropuesta.tipoPropuesta1)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idPropuesta }) |
                @Html.ActionLink("Details", "Details", new { id=item.idPropuesta }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idPropuesta })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>

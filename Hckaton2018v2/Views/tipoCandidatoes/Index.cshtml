@model IEnumerable<Hckaton2018v2.tipoCandidato>

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>
    <p>
        @Html.ActionLink("Create New", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.tipoCandidato1)
            </th>
            <th></th>
        </tr>
    
    @foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.tipoCandidato1)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id=item.idTipoCandidato }) |
                @Html.ActionLink("Details", "Details", new { id=item.idTipoCandidato }) |
                @Html.ActionLink("Delete", "Delete", new { id=item.idTipoCandidato })
            </td>
        </tr>
    }
    
    </table>
</body>
</html>

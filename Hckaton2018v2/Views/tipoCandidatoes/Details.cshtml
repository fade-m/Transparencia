@model Hckaton2018v2.tipoCandidato

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Details</title>
</head>
<body>
    <div>
        <h4>tipoCandidato</h4>
        <hr />
        <dl class="dl-horizontal">
            <dt>
                @Html.DisplayNameFor(model => model.tipoCandidato1)
            </dt>
    
            <dd>
                @Html.DisplayFor(model => model.tipoCandidato1)
            </dd>
    
        </dl>
    </div>
    <p>
        @Html.ActionLink("Edit", "Edit", new { id = Model.idTipoCandidato }) |
        @Html.ActionLink("Back to List", "Index")
    </p>
</body>
</html>
